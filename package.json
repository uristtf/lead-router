{
  "name": "lead-router",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "axios": "^1.6.0"
  }
}
```

---

### File 2 — `Procfile` (no extension, just that name)

Create a new file called exactly `Procfile` (capital P, no `.txt` or anything after it) and paste this:
```
web: node server.js
